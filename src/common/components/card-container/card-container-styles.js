import { StyleSheet } from 'react-native'

export function createStyles () {
    return StyleSheet.create({
        container: {
            flexDirection: 'row',
            alignContent: 'center',
            flexWrap: 'wrap'
        }
    })
}
